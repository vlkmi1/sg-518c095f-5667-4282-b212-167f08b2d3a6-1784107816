<![CDATA[
import { supabase } from "@/integrations/supabase/client";
import type { Tables } from "@/
...
    console.log("getCompetitionResults:", { results, error });
    return results;
  },
};
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 6697 chars.]